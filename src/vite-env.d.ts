/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Build time, injected by Vite. Shown in the menu so the running build is identifiable. */
declare const __BUILD_STAMP__: string
